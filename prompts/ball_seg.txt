请对图像中篮球运动物体及篮球相关设施进行实例分割，要求：

1. 输出结构：
生成符合RFC8259标准的JSON数组，每个对象包含：
- "box_2d": [x_min, y_min, x_max, y_max] 格式的归一化边界框坐标（相对图像尺寸）
- "label": 符合ISO 3166标准的描述性中文标签

2. 分类规范：
- 球类标签：篮球
- 设施标签：篮筐完整结构
- 对遮挡物体进行完整外推标注
- 如果找不到球类, 返回空内容

3. 质量控制：
- 最小检测尺寸：32×32像素
- IoU阈值：同类物体>0.7时合并
- 置信度过滤：仅保留score>0.85的预测

4. 异常处理：
- 未识别物体标记为"unknown_ball"或"unknown_hoop"
- 多球重叠时建立父子层级关系
- 提供置信度分数作为可选字段
- 请确保坐标系统一为左上角原点，数值精确到小数点后四位，并添加版本标识符。
- 如果找不到任何球类, 返回空内容